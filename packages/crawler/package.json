{
  "name": "@autowp/crawler",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@autowp/browser": "workspace:*",
    "@autowp/event-bus": "workspace:*",
    "@autowp/html-parser": "workspace:*",
    "@autowp/queue": "workspace:*"
  }
}
