# @autowp/browser

A browser session pool abstraction package for future Playwright integration.

## API

- `BrowserAdapter<TSession>`: generic browser adapter interface.
- `BrowserPool<TSession>`: pool that manages browser sessions and concurrency.

## Usage

```ts
import { BrowserPool } from '@autowp/browser';

// Provide an adapter implementation for the chosen browser runtime.
```
